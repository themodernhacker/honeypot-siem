# Cowrie user database — controls which SSH logins succeed vs. fail.
# Format:  username:x:password    (a leading ! denies; * matches anything)
# First matching line wins. Lines below have no trailing '*', so any password
# not explicitly allowed will FAIL — giving us deterministic brute-force demos.
#
# Result for this lab:
#   root + any of the denied passwords below  -> login.failed  (feeds rule 100104)
#   root / hunter2                            -> login.success (feeds rule 100105)

# --- Denied (these will fail) ---
root:x:!admin
root:x:!password
root:x:!123456
root:x:!12345
root:x:!root
root:x:!toor
root:x:!qwerty
root:x:!letmein
root:x:!changeme
root:x:!000000

# --- Allowed (this one succeeds) ---
root:x:hunter2
